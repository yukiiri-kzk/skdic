<h2>アイテム一覧</h2>

  <% @items.each do |item| %>
    <div>
      <%= image_tag item.image.url, size: "100x100" if item.image? %>
      <%= link_to item.name, item_path(item.user) %>
      <%= link_to item.user.name, user_path(item.user) %>
    </div>
  <% end %>
